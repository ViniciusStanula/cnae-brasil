---
interface Props {
  slot?: 'top' | 'mid' | 'bottom' | 'sidebar';
}

const { slot: slotName = 'mid' } = Astro.props;
---

<!-- AdSense slot: {slotName} — activate after AdSense approval -->
<div class="ad-slot my-6 min-h-[90px] bg-gray-50 border border-dashed border-gray-200 rounded flex items-center justify-center" aria-hidden="true">
  <span class="text-xs text-gray-300 select-none">publicidade</span>
</div>
